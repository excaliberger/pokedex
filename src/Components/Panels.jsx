import { useEffect, useState } from 'react';
import { getPokemonStats, filterPokesByType, filterPokesByWeakness} from '../helpers/pokedex.helpers'


function Panels({ id, name, type, num, img }) {

    return (
        <div>
            <p>hello there</p>
        </div>
    )

}


export default Panels;